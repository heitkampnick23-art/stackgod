name: Android Build
on:
  workflow_dispatch:
    inputs:
      app_id: { required: true, type: string }
      app_name: { required: true, type: string }
      package: { required: true, type: string }

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: 'temurin', java-version: '17' }
      - name: Render Compose template + build .aab
        run: echo "TODO render templates and run ./gradlew bundleRelease"
      - name: Upload to Play internal track
        env:
          PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
        run: echo "TODO use google-api-python-client edits.upload + commit to internal track"
